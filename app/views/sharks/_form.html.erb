<%= form_for(@shark) do |f| %>
  <% if @shark.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@shark.errors.count, "error") %> prohibited this shark from being saved:</h2>

      <ul>
      <% @shark.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <center><%= image_tag @shark.avatar.url(:avatar) %></center>
  <div class="field">
    <%= f.label :avatar %><br>
    <%= f.file_field :avatar %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
